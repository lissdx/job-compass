# Copy to .env and fill in. Never commit .env — see .gitignore.

# Path to the SQLite database file.
JOB_COMPASS_DB=db/sqlite/jobcompass.db

# LLM provider key, once the conversational layer lands.
# ANTHROPIC_API_KEY=
